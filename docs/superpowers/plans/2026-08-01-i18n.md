# Lokalisierung (DE/EN/FR/ES/JA) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Die rein deutsche UI der Tsundoku-App wird in fünf Sprachen nutzbar (Deutsch, Englisch, Französisch, Spanisch, Japanisch), inklusive japanischer Typografie und englischer README.

**Architecture:** Eigene typisierte Message-Module ohne i18n-Bibliothek: ein `Messages`-Interface als einzige Wahrheit über alle Schlüssel, fünf Bundles (`de.tsx` als Referenz), ein React-Context (`useI18n()`) mit locale-gebundenem Zahlenformat. Sprachwahl per Auto-Detect + Umschalter in der Fußzeile, Persistenz in localStorage nur bei expliziter Wahl. Japanische Typografie über CSS-Token-Overrides in einem `:lang(ja)`-Block.

**Tech Stack:** React 19 + TypeScript (strict), Vitest, Fontsource (neu: `@fontsource/shippori-mincho`, `@fontsource/noto-serif-jp`), `Intl.NumberFormat`/`Intl.DisplayNames`.

**Spec:** `docs/superpowers/specs/2026-08-01-i18n-design.md` — bei Widersprüchen gilt die Spec.

## Global Constraints

- **Sprachen:** `de`, `en`, `fr`, `es`, `ja`; Fallback ohne Treffer ist `en`. Deutsch ist Referenz: die heutigen Texte wandern **wörtlich unverändert** nach `de.tsx`.
- **Keine neuen Runtime-Dependencies** außer `@fontsource/shippori-mincho` und `@fontsource/noto-serif-jp`.
- **localStorage-Schlüssel:** `tsundoku.locale`; wird **nur** beim expliziten Umschalten geschrieben, nie beim Auto-Detect.
- **Sprache nicht in der URL.** Der Dokumenttitel „Tsundoku 積ん読" bleibt in allen Sprachen gleich.
- **Konvention Funktions-Messages:** Zahlen werden **vorformatiert als String** übergeben (Parametername endet auf `Fmt`, formatiert vom Aufrufer mit `fmtInt` aus `useI18n()`); Jahreszahlen als `number` (ohne Tausendertrennung). Messages dürfen `ReactNode` zurückgeben (Bundles sind `.tsx`).
- **Datenwerte werden nie übersetzt:** Tags, Sammlungsnamen, Autor·innen, Buchtitel, Listennamen bleiben roh.
- **Register:** DE Du-Form (Bestand), EN neutral-direkt, FR Vouvoiement, ES Tuteo, JA です/ます-Form.
- Nach jedem Task: `npm test` grün und `npx tsc --noEmit` fehlerfrei. UI-Texte niemals hart in Komponenten — alles über `useI18n()`.
- Kommentare im Code auf Englisch, außer sie erklären Datenkuriositäten (Projektkonvention).

## File Structure

```
src/i18n/messages.ts        Interface + SUPPORTED_LOCALES + LOCALE_NAMES (Task 1)
src/i18n/de.tsx             Referenz-Bundle (Task 1)
src/i18n/bundles.ts         BUNDLES: Record<Locale, Messages> (Task 1; EN/FR/ES/JA zunächst = de)
src/i18n/detect.ts          detectLocale() pur (Task 1)
src/i18n/detect.test.ts     (Task 1)
src/i18n/bundles.test.ts    Rauchtest aller Bundles (Task 1)
src/i18n/LocaleContext.tsx  Provider + useI18n() (Task 1)
src/i18n/en.tsx fr.tsx es.tsx ja.tsx   Übersetzungen (Tasks 6–9)
src/lib/languages.ts        LANG_ISO + Intl.DisplayNames statt LANG_LABELS (Task 2)
src/store/filters.ts        filterLabel(f, m) (Task 3)
src/App.tsx, src/components/*  auf useI18n() (Tasks 3–4)
src/views/*                 auf useI18n() (Task 5)
src/styles/tokens.css, global.css, main.tsx, package.json   Typografie (Task 11)
README.md, CLAUDE.md        englische README, Konventionen (Task 12)
```

---

### Task 1: i18n-Fundament (Interface, de-Bundle, Detection, Context)

**Files:**
- Create: `src/i18n/messages.ts`, `src/i18n/de.tsx`, `src/i18n/bundles.ts`, `src/i18n/detect.ts`, `src/i18n/LocaleContext.tsx`
- Test: `src/i18n/detect.test.ts`, `src/i18n/bundles.test.ts`
- Modify: `src/main.tsx` (Provider um `<App />`)

**Interfaces:**
- Consumes: `ViewId`, `MediaType` aus `src/lib/types.ts`.
- Produces (spätere Tasks verlassen sich exakt hierauf):
  - `type Locale = 'de' | 'en' | 'fr' | 'es' | 'ja'`, `SUPPORTED_LOCALES`, `LOCALE_NAMES: Record<Locale, string>`
  - `interface Messages` (vollständig unten)
  - `detectLocale(stored: string | null, navigatorLangs: readonly string[]): Locale`
  - `useI18n(): { locale: Locale; m: Messages; fmtInt: (n: number) => string; setLocale: (l: Locale) => void }`
  - `BUNDLES: Record<Locale, Messages>`

- [ ] **Step 1: Failing Tests für `detectLocale` schreiben**

`src/i18n/detect.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { detectLocale } from './detect'

describe('detectLocale', () => {
  it('nimmt die gespeicherte Wahl vor der Browsersprache', () => {
    expect(detectLocale('ja', ['de-DE', 'de'])).toBe('ja')
  })
  it('ignoriert ungültige gespeicherte Werte', () => {
    expect(detectLocale('tlh', ['fr-FR'])).toBe('fr')
    expect(detectLocale('', ['fr-FR'])).toBe('fr')
  })
  it('matcht Browsersprachen per Prefix in Reihenfolge', () => {
    expect(detectLocale(null, ['de-AT', 'en-US'])).toBe('de')
    expect(detectLocale(null, ['pt-BR', 'es-419', 'en'])).toBe('es')
    expect(detectLocale(null, ['ja'])).toBe('ja')
  })
  it('fällt ohne Treffer auf en zurück', () => {
    expect(detectLocale(null, ['pt-BR', 'zh-CN'])).toBe('en')
    expect(detectLocale(null, [])).toBe('en')
  })
})
```

- [ ] **Step 2: Test laufen lassen, Fehlschlag verifizieren**

Run: `npx vitest run src/i18n/detect.test.ts`
Expected: FAIL („Cannot find module './detect'")

- [ ] **Step 3: `src/i18n/messages.ts` schreiben — das vollständige Interface**

```ts
import type { ReactNode } from 'react'
import type { MediaType, ViewId } from '../lib/types'

export const SUPPORTED_LOCALES = ['de', 'en', 'fr', 'es', 'ja'] as const
export type Locale = (typeof SUPPORTED_LOCALES)[number]

/** Endonyme für den Umschalter — bewusst in keiner Sprache übersetzt. */
export const LOCALE_NAMES: Record<Locale, string> = {
  de: 'Deutsch',
  en: 'English',
  fr: 'Français',
  es: 'Español',
  ja: '日本語',
}

/**
 * Alle UI-Texte der App. Konventionen:
 * - Zahlen kommen vorformatiert als String an (Parameter `…Fmt`),
 *   Jahreszahlen als number (keine Tausendertrennung).
 * - Schlüssel mit Markup liefern ReactNode (Bundles sind .tsx).
 * - Datenwerte (Tags, Autor·innen, Titel, Listennamen) werden nie übersetzt.
 */
export interface Messages {
  locale: Locale
  app: {
    loading: string
    loadError: (message: string) => string
    incompatibleNotice: string
    replaceLibrary: string
    navAria: string
  }
  nav: Record<ViewId, string>
  upload: {
    intro: ReactNode
    title: string
    ltIntro: ReactNode
    dropHere: string
    chooseFile: string
    working: string
    backToLoaded: string
    errorPrefix: string
    errInvalidJson: string
    errTooLarge: (mbFmt: string, limitMbFmt: string) => string
    errTooMany: (maxFmt: string) => string
    errNotAnExport: string
    errNoBooks: string
  }
  report: {
    title: string
    note: string
    entries: string
    media: string
    read: string
    readValue: (readFmt: string, knownFmt: string, datedFmt: string, minYear: number | null) => string
    pagesTotal: string
    readDays: string
    readDaysValue: (median: number, p90: number, max: number) => string
    tags: string
    tagsValue: (normFmt: string, rawFmt: string) => string
    dimsSwapped: string
    dimsSwappedValue: (sortedFmt: string, discardedFmt: string) => string
    dimsEstimated: string
    dimsEstimatedValue: (nFmt: string) => string
    origLangInferred: string
    origLangInferredValue: (nFmt: string) => string
    entitiesDecoded: string
    entitiesDecodedValue: (nFmt: string) => string
    bulkImport: string
    bulkImportValue: (nFmt: string) => string
    toLibrary: string
    otherFile: string
  }
  media: Record<MediaType, string>
  ddc: {
    labels: Record<number, string>
    short: Record<number, string>
  }
  lang: {
    other: string
    unknown: string
  }
  filter: {
    tag: (v: string) => string
    language: (label: string) => string
    originalLanguage: (label: string) => string
    ddcTop: (label: string) => string
    mediaType: (label: string) => string
    collection: (v: string) => string
    author: (v: string) => string
    award: (v: string) => string
    acquired: (from: number, to: number) => string
    read: (from: number, to: number) => string
    edition: (from: number, to: number) => string
    statusRead: string
    statusUnread: string
  }
  chips: {
    regionAria: string
    removeAria: (label: string) => string
    clearAll: string
  }
  empty: {
    title: string
    active: string
    release: string
  }
  summary: {
    titles: string
    read: string
    pages: string
    filteredFrom: (totalFmt: string) => string
  }
  coverage: {
    frame: (covered: ReactNode, total: ReactNode, unit: string) => ReactNode
    unitTitles: string
    unitTags: string
  }
  detail: {
    original: string
    editionYear: string
    language: string
    originalLanguage: string
    pages: string
    ddc: string
    acquired: string
    read: string
    readTagged: (year: number) => string
    rating: string
    boughtAt: string
    series: string
    isbn: string
    tags: string
    toggleFilterAria: (label: string) => string
    filterByAuthorAria: (name: string) => string
    viewOnLt: string
    close: string
  }
  rangeForm: {
    dimensionAria: string
    acquired: string
    read: string
    from: string
    to: string
    submit: string
  }
  views: {
    shelf: {
      title: string
      coverage: (estimatedFmt: ReactNode, unmeasuredFmt: ReactNode, nonBooksFmt: ReactNode) => ReactNode
      sort: string
      color: string
      sortLabels: { acquired: string; author: string; height: string; ddc: string }
      colorLabels: { ddc: string; language: string; readStatus: string; acquiredYear: string }
      svgAria: (countFmt: string) => string
      estimatedSuffix: string
      estimatedShort: string
      unmeasuredAria: string
      unmeasuredTitle: (countFmt: string) => string
      legendAria: string
      noInfo: string
      legendRead: string
      legendUnread: string
      noAcqYear: string
      decade: (decade: number) => string
    }
    timeline: {
      title: string
      coverage: (readKnownFmt: ReactNode, taggedOnlyFmt: ReactNode) => ReactNode
      noYears: string
      svgAria: string
      maxGap: (year: number) => string
      brushAcquired: string
      brushRead: string
      unreadSvgAria: string
      unreadPanelLabel: string
      legendAcquired: string
      legendReadDated: string
      legendReadTagged: string
      filterUnread: string
      hint: string
      tooltipAcquired: (countFmt: string) => string
      andMore: (countFmt: string) => string
      tooltipUnread: (countFmt: string) => string
    }
    knowledge: {
      title: string
      coverage: (deltaFmt: ReactNode) => ReactNode
      noData: string
      controlsAria: string
      absolute: string
      share: string
      smooth: string
      svgAria: string
      streamTitle: (ddcClass: number, label: string, countFmt: string) => string
      hint: string
    }
    network: {
      title: string
      coverage: (minCount: number, yearTagsFmt: ReactNode, statusFmt: ReactNode, seriesFmt: ReactNode) => ReactNode
      minCountLabel: string
      searchPlaceholder: string
      searchAria: string
      zoomLabel: string
      zoomAria: string
      fit: string
      unisolate: (tag: string) => string
      svgAria: string
      nodeAria: (tag: string, countFmt: string) => string
      nodeTitle: (tag: string, countFmt: string) => string
    }
    languages: {
      title: string
      coverage: (inferredFmt: ReactNode) => ReactNode
      noData: string
      svgAria: string
      linkLabel: (source: string, target: string, countFmt: string) => string
      linkAriaFilter: (label: string) => string
      origSide: string
      edSide: string
      nodeAria: (side: string, lang: string, countFmt: string) => string
      nodeTitle: (side: string, lang: string, countFmt: string) => string
    }
    years: {
      title: string
      coverage: ReactNode
      noData: string
      underflow: (countFmt: string) => string
      svgAria: string
      axisEdition: string
      axisAcquired: string
      edition: string
      acquired: string
      edFromAria: string
      edToAria: string
      acqFromAria: string
      acqToAria: string
      submit: string
      tooltip: (ed: number, acq: number, countFmt: string) => string
    }
    pace: {
      title: string
      coverage: ReactNode
      discarded: (countFmt: ReactNode) => ReactNode
      noData: (pointsFmt: ReactNode) => ReactNode
      facetToggle: string
      svgAria: (langLabel: string | null) => string
      rateLabel: (rate: number) => string
      dotAria: (title: string, pagesFmt: string, daysFmt: string) => string
      dotTitle: (title: string, pagesFmt: string, daysFmt: string, suspect: boolean) => string
      axisPages: string
      axisDays: string
      note: string
    }
    canon: {
      title: string
      coverage: string
      noData: string
      showLists: string
      onlyUnread: string
      counts: (ownedFmt: string, readFmt: string) => string
    }
  }
  footer: {
    license: string
    embedded: string
    languageAria: string
  }
}
```

- [ ] **Step 4: `src/i18n/de.tsx` schreiben — die Referenzfassung**

Die Strings sind wörtlich aus dem heutigen Code übernommen (Quelle in Klammern
nur zur Orientierung des Reviewers; nicht in den Code übernehmen):

```tsx
import type { Messages } from './messages'

export const de: Messages = {
  locale: 'de',
  app: {
    loading: 'Bibliothek wird geladen …',
    loadError: (message) => `Bibliothek konnte nicht geladen werden: ${message}`,
    incompatibleNotice:
      'Die im Browser gespeicherte Bibliothek stammt aus einer älteren Version der Anwendung ' +
      'und kann nicht mehr gelesen werden — bitte den Export einmal neu hochladen.',
    replaceLibrary: 'Bibliothek wechseln',
    navAria: 'Ansichten',
  },
  nav: {
    shelf: 'Regal',
    timeline: 'Erwerb & Lektüre',
    knowledge: 'Wissenslandkarte',
    network: 'Tag-Netzwerk',
    languages: 'Sprachfluss',
    years: 'Ausgabe × Erwerb',
    pace: 'Lesetempo',
    canon: 'Kanon',
  },
  upload: {
    intro: (
      <>
        <em>Tsundoku</em> (積ん読) — Bücher kaufen und stapeln, ohne sie zu lesen. Diese Anwendung
        erkundet eine LibraryThing-Bibliothek interaktiv: acht verknüpfte Ansichten, vom
        maßstabsgetreu gezeichneten Regal über Zeitleisten und Tag-Netzwerk bis zum Sprachfluss —
        und jede Ansicht ist zugleich Filter für alle anderen. Die zentrale Frage dabei: Was
        verrät die Differenz zwischen dem, was man <em>erwirbt</em>, und dem, was man{' '}
        <em>liest</em>?
      </>
    ),
    title: 'Bibliothek laden',
    ltIntro: (
      <>
        Die Anwendung liest Exporte von{' '}
        <a href="https://www.librarything.com" target="_blank" rel="noopener noreferrer">
          LibraryThing
        </a>
        , einem Online-Dienst zum Katalogisieren der eigenen Büchersammlung. Exportiere deine
        Bibliothek dort auf{' '}
        <a href="https://www.librarything.com/export.php" target="_blank" rel="noopener noreferrer">
          librarything.com/export.php
        </a>{' '}
        im Format <strong>JSON</strong> und lade die Datei hier. Sie wird direkt im Browser
        eingelesen und <strong>verlässt deinen Rechner nicht</strong>.
      </>
    ),
    dropHere: 'Export-Datei hierher ziehen oder',
    chooseFile: 'Datei auswählen',
    working: 'Wird normalisiert …',
    backToLoaded: 'Zurück zur geladenen Bibliothek',
    errorPrefix: 'Fehler:',
    errInvalidJson: 'Datei ist kein gültiges JSON.',
    errTooLarge: (mbFmt, limitMbFmt) =>
      `Datei ist ${mbFmt} MB groß — Obergrenze ${limitMbFmt} MB. LibraryThing erlaubt gefilterte Exporte (z. B. eine Sammlung).`,
    errTooMany: (maxFmt) =>
      `der Export enthält mehr als ${maxFmt} Einträge. ` +
      'Die Ansichten halten alles im Speicher; bitte einen gefilterten Export wählen (LibraryThing kann z. B. nach Sammlung exportieren).',
    errNotAnExport: 'kein LibraryThing-Export (erwartet: JSON-Objekt mit Buch-IDs als Schlüsseln)',
    errNoBooks: 'kein LibraryThing-Export — die Datei enthält keine Buch-Einträge',
  },
  report: {
    title: 'Deine Bibliothek ist bereit',
    note:
      'Beim Einlesen wurden kleine Unstimmigkeiten des Katalogs behoben — etwa vertauschte ' +
      'Buchmaße, fehlende Angaben oder kaputte Sonderzeichen. Nichts davon passiert im ' +
      'Verborgenen: Die Übersicht zeigt, was mit deinen Daten geschehen ist.',
    entries: 'Einträge',
    media: 'Medien',
    read: 'Gelesen',
    readValue: (readFmt, knownFmt, datedFmt, minYear) =>
      `${readFmt} (Lesejahr bekannt: ${knownFmt}, davon ${datedFmt} tagesgenau${minYear !== null ? `, ab ${minYear}` : ''})`,
    pagesTotal: 'Seiten gesamt',
    readDays: 'Lesedauer',
    readDaysValue: (median, p90, max) => `meist ${median} Tage, selten über ${p90}, längste ${max}`,
    tags: 'Tags',
    tagsValue: (normFmt, rawFmt) => `${normFmt} vereinheitlicht (im Export: ${rawFmt})`,
    dimsSwapped: 'Vertauschte Buchmaße',
    dimsSwappedValue: (sortedFmt, discardedFmt) => `${sortedFmt} korrigiert, ${discardedFmt} verworfen`,
    dimsEstimated: 'Geschätzte Buchmaße',
    dimsEstimatedValue: (nFmt) => `${nFmt} Bücher (aus der Seitenzahl)`,
    origLangInferred: 'Originalsprache ergänzt',
    origLangInferredValue: (nFmt) => `${nFmt} Bücher (aus der Ausgabesprache)`,
    entitiesDecoded: 'Sonderzeichen repariert',
    entitiesDecodedValue: (nFmt) => `${nFmt} Felder`,
    bulkImport: 'Massenimport erkannt',
    bulkImportValue: (nFmt) => `${nFmt} Einträge`,
    toLibrary: 'Zur Bibliothek',
    otherFile: 'Andere Datei wählen',
  },
  media: {
    book: 'Buch',
    ebook: 'E-Book',
    film: 'Film',
    vinyl: 'Schallplatte',
  },
  ddc: {
    labels: {
      0: 'Allgemeines & Informatik',
      1: 'Philosophie & Psychologie',
      2: 'Religion',
      3: 'Sozialwissenschaften',
      4: 'Sprache',
      5: 'Naturwissenschaften',
      6: 'Technik & Medizin',
      7: 'Künste & Unterhaltung',
      8: 'Literatur',
      9: 'Geschichte & Geographie',
    },
    short: {
      0: 'Informatik',
      1: 'Philosophie',
      2: 'Religion',
      3: 'Sozialwissenschaften',
      4: 'Sprache',
      5: 'Naturwissenschaften',
      6: 'Technik',
      7: 'Künste',
      8: 'Literatur',
      9: 'Geschichte',
    },
  },
  lang: {
    other: 'andere',
    unknown: 'unbekannt',
  },
  filter: {
    tag: (v) => `Tag: ${v}`,
    language: (label) => `Sprache: ${label}`,
    originalLanguage: (label) => `Original: ${label}`,
    ddcTop: (label) => `Wissensgebiet: ${label}`,
    mediaType: (label) => `Medium: ${label}`,
    collection: (v) => `Sammlung: ${v}`,
    author: (v) => `Autor·in: ${v}`,
    award: (v) => `Liste: ${v}`,
    acquired: (from, to) => `Erworben: ${from}–${to}`,
    read: (from, to) => `Gelesen: ${from}–${to}`,
    edition: (from, to) => `Ausgabe: ${from}–${to}`,
    statusRead: 'Status: gelesen',
    statusUnread: 'Status: ungelesen',
  },
  chips: {
    regionAria: 'Aktive Filter',
    removeAria: (label) => `Filter entfernen: ${label}`,
    clearAll: 'Alle Filter lösen',
  },
  empty: {
    title: 'Keine Titel im aktuellen Filter',
    active: 'Diese Filter greifen gerade:',
    release: 'lösen',
  },
  summary: {
    titles: 'Titel',
    read: 'davon gelesen',
    pages: 'Seiten',
    filteredFrom: (totalFmt) => `gefiltert aus ${totalFmt} Titeln`,
  },
  coverage: {
    frame: (covered, total, unit) => (
      <>
        {covered} von {total} {unit}
      </>
    ),
    unitTitles: 'Titeln',
    unitTags: 'Tags',
  },
  detail: {
    original: 'Original',
    editionYear: 'Jahr dieser Ausgabe',
    language: 'Sprache',
    originalLanguage: 'Originalsprache',
    pages: 'Seiten',
    ddc: 'Wissensgebiet',
    acquired: 'Erworben',
    read: 'Gelesen',
    readTagged: (year) => `${year} (Jahres-Tag)`,
    rating: 'Bewertung',
    boughtAt: 'Gekauft bei',
    series: 'Reihe',
    isbn: 'ISBN',
    tags: 'Tags',
    toggleFilterAria: (label) => `Filter ${label} umschalten`,
    filterByAuthorAria: (name) => `Nach ${name} filtern`,
    viewOnLt: 'Auf LibraryThing ansehen ↗',
    close: 'Schließen',
  },
  rangeForm: {
    dimensionAria: 'Dimension des Zeitraumfilters',
    acquired: 'Erwerb',
    read: 'Lektüre',
    from: 'von',
    to: 'bis',
    submit: 'Zeitraum filtern',
  },
  views: {
    shelf: {
      title: 'Das Regal',
      coverage: (estimatedFmt, unmeasuredFmt, nonBooksFmt) => (
        <>
          stehen im Regal — davon {estimatedFmt} mit aus der Seitenzahl geschätzten Maßen
          (halbtransparent, gestrichelte Kontur). {unmeasuredFmt} ohne Maße und Seitenzahl unten,{' '}
          {nonBooksFmt} Nicht-Bücher nicht dargestellt.
        </>
      ),
      sort: 'Sortierung',
      color: 'Farbe',
      sortLabels: { acquired: 'Erwerb', author: 'Autor·in', height: 'Höhe', ddc: 'Wissensgebiet' },
      colorLabels: { ddc: 'Wissensgebiet', language: 'Sprache', readStatus: 'Lesestatus', acquiredYear: 'Erwerbsjahr' },
      svgAria: (countFmt) => `Regal mit ${countFmt} Büchern`,
      estimatedSuffix: ' (Maße geschätzt)',
      estimatedShort: 'Maße geschätzt',
      unmeasuredAria: 'Bücher ohne Maßangaben',
      unmeasuredTitle: (countFmt) =>
        `ohne Maße und ohne Seitenzahl zur Schätzung (${countFmt}) — Einheitsgröße, nicht maßstäblich`,
      legendAria: 'Farblegende',
      noInfo: 'ohne Angabe',
      legendRead: 'gelesen',
      legendUnread: 'ungelesen (Kontur)',
      noAcqYear: 'ohne Erwerbsjahr',
      decade: (decade) => `${decade}er`,
    },
    timeline: {
      title: 'Erwerb und Lektüre',
      coverage: (readKnownFmt, taggedOnlyFmt) => (
        <>
          haben ein Erwerbsjahr; {readKnownFmt} ein Lesejahr, davon {taggedOnlyFmt} nur über
          Jahres-Tags.
        </>
      ),
      noYears: 'im aktuellen Filter haben ein Erwerbs- oder Lesejahr.',
      svgAria: 'Erwerb (nach oben) und Lektüre (nach unten) pro Jahr',
      maxGap: (year) => `größte Schere: ${year}`,
      brushAcquired: 'Erwerb',
      brushRead: 'Lektüre',
      unreadSvgAria: 'Ungelesener Bestand, kumulativ',
      unreadPanelLabel: 'ungelesener Bestand (nur Titel mit Erwerbsjahr)',
      legendAcquired: 'Erwerb',
      legendReadDated: 'Lektüre (tagesgenau)',
      legendReadTagged: 'Lektüre (Jahres-Tag)',
      filterUnread: 'Ungelesene filtern',
      hint:
        'Zeitraum wählen: im Diagramm horizontal ziehen — über der Nulllinie filtert nach Erwerbsjahr, ' +
        'darunter nach Lesejahr; ein Klick wählt ein einzelnes Jahr, Esc bricht die Auswahl ab.',
      tooltipAcquired: (countFmt) => `${countFmt} erworben`,
      andMore: (countFmt) => `… und ${countFmt} weitere`,
      tooltipUnread: (countFmt) => `${countFmt} ungelesen im Bestand`,
    },
    knowledge: {
      title: 'Wissenslandkarte',
      coverage: (deltaFmt) => (
        <>haben DDC-Code und Erwerbsjahr ({deltaFmt} mit Erwerbsjahr, aber ohne DDC).</>
      ),
      noData: 'im aktuellen Filter haben DDC-Code und Erwerbsjahr.',
      controlsAria: 'Darstellung',
      absolute: 'absolut',
      share: 'Anteile',
      smooth: 'Dreijahresschnitt',
      svgAria: 'DDC-Hauptklassen über Erwerbsjahre',
      streamTitle: (ddcClass, label, countFmt) => `${ddcClass} ${label}: ${countFmt} Titel`,
      hint:
        'Zeitraum wählen: horizontal über das Diagramm ziehen filtert nach Erwerbsjahr, Esc bricht die ' +
        'Auswahl ab; ein Klick auf einen Strom filtert nach dem Wissensgebiet.',
    },
    network: {
      title: 'Tag-Netzwerk',
      coverage: (minCount, yearTagsFmt, statusFmt, seriesFmt) => (
        <>
          haben ≥ {minCount} Titel und sind im Netz; ausgeblendet: {yearTagsFmt} Jahres-Tags,{' '}
          {statusFmt} Statusmarker, {seriesFmt} Reihenkürzel.
        </>
      ),
      minCountLabel: 'Mindestanzahl Titel:',
      searchPlaceholder: 'Tag suchen …',
      searchAria: 'Tag suchen',
      zoomLabel: 'Zoom:',
      zoomAria: 'Zoomfaktor',
      fit: 'Einpassen',
      unisolate: (tag) => `Isolation aufheben (${tag})`,
      svgAria: 'Netzwerk gemeinsam vergebener Tags',
      nodeAria: (tag, countFmt) => `Tag ${tag}, ${countFmt} Titel`,
      nodeTitle: (tag, countFmt) =>
        `${tag}: ${countFmt} Titel (Klick = filtern, Shift-Klick = Nachbarschaft isolieren)`,
    },
    languages: {
      title: 'Sprachfluss',
      coverage: (inferredFmt) => (
        <>
          haben eine Ausgabesprache; bei {inferredFmt} davon gilt sie mangels erfasster
          Originalsprache zugleich als Original (Erfassungskonvention).
        </>
      ),
      noData: 'im aktuellen Filter haben eine Ausgabesprache.',
      svgAria: 'Fluss von Originalsprache zu Ausgabesprache',
      linkLabel: (source, target, countFmt) => `${source} → ${target}: ${countFmt} Titel`,
      linkAriaFilter: (label) => `${label}. Enter filtert auf diese Kombination.`,
      origSide: 'Originalsprache',
      edSide: 'Ausgabesprache',
      nodeAria: (side, lang, countFmt) => `${side} ${lang}, ${countFmt} Titel`,
      nodeTitle: (side, lang, countFmt) => `${side} ${lang}: ${countFmt} Titel (Klick filtert nur diese Sprache)`,
    },
    years: {
      title: 'Ausgabejahr gegen Erwerbsjahr',
      coverage: (
        <>
          haben beide Jahre. Achtung: das ist das Jahr <em>dieser Ausgabe</em>, nicht des Werks —
          eine Reclam-Sophokles-Ausgabe zählt als 1998.
        </>
      ),
      noData: 'im aktuellen Filter haben Ausgabe- und Erwerbsjahr (ab 1900).',
      underflow: (countFmt) => `${countFmt} Ausgaben vor 1900 nicht dargestellt.`,
      svgAria: 'Heatmap Ausgabejahr × Erwerbsjahr',
      axisEdition: 'Jahr dieser Ausgabe →',
      axisAcquired: 'Erwerbsjahr →',
      edition: 'Ausgabe',
      acquired: 'Erwerb',
      edFromAria: 'Ausgabejahr von',
      edToAria: 'Ausgabejahr bis',
      acqFromAria: 'Erwerbsjahr von',
      acqToAria: 'Erwerbsjahr bis',
      submit: 'Bereich filtern',
      tooltip: (ed, acq, countFmt) => `Ausgabe ${ed}, erworben ${acq}: ${countFmt} Titel`,
    },
    pace: {
      title: 'Lesetempo',
      coverage: 'haben Lesedauer und Seitenzahl — überproportional die bewusst getrackten.',
      discarded: (countFmt) => <>{countFmt} negative Dauern verworfen.</>,
      noData: (pointsFmt) => (
        <>
          im aktuellen Filter haben Start- und Enddatum (davon {pointsFmt} auch eine Seitenzahl).
        </>
      ),
      facetToggle: 'nach Sprache facettieren',
      svgAria: (langLabel) => `Seiten gegen Lesedauer${langLabel ? `, ${langLabel}` : ''}`,
      rateLabel: (rate) => `${rate} S./Tag`,
      dotAria: (title, pagesFmt, daysFmt) => `${title}: ${pagesFmt} Seiten in ${daysFmt} Tagen`,
      dotTitle: (title, pagesFmt, daysFmt, suspect) =>
        `${title} — ${pagesFmt} S. / ${daysFmt} Tage${suspect ? ' (über 100 Tage: offen, ob durchgehend gelesen)' : ''}`,
      axisPages: 'Seiten',
      axisDays: 'Tage',
      note: 'Hohle Punkte: über 100 Tage — offen, ob durchgehend gelesen; nicht als Tempo interpretieren.',
    },
    canon: {
      title: 'Kanonabgleich',
      coverage:
        'stehen auf mindestens einer Liste. Angaben sind „im Bestand", nicht „von der ' +
        'Liste erledigt" — der Listenumfang ist aus dem Export nicht bekannt.',
      noData: 'im aktuellen Filter stehen auf einer Auszeichnungs- oder Kanonliste.',
      showLists: 'Listen anzeigen:',
      onlyUnread: 'Nur Ungelesene → Leseliste',
      counts: (ownedFmt, readFmt) => `${ownedFmt} im Bestand · ${readFmt} gelesen`,
    },
  },
  footer: {
    license: 'MIT-Lizenz',
    embedded: 'Lizenzen eingebetteter Schriften & Bibliotheken',
    languageAria: 'Sprache',
  },
}
```

- [ ] **Step 5: `src/i18n/detect.ts` schreiben**

```ts
import { SUPPORTED_LOCALES, type Locale } from './messages'

function isLocale(v: string): v is Locale {
  return (SUPPORTED_LOCALES as readonly string[]).includes(v)
}

/** Gespeicherte Wahl gewinnt; sonst erster Prefix-Treffer aus den
 *  Browsersprachen; sonst 'en'. Pur gehalten für Testbarkeit. */
export function detectLocale(stored: string | null, navigatorLangs: readonly string[]): Locale {
  if (stored !== null && isLocale(stored)) return stored
  for (const lang of navigatorLangs) {
    const prefix = lang.toLowerCase().split('-')[0]
    if (isLocale(prefix)) return prefix
  }
  return 'en'
}
```

- [ ] **Step 6: Detect-Tests laufen lassen**

Run: `npx vitest run src/i18n/detect.test.ts`
Expected: PASS (4 Tests)

- [ ] **Step 7: `src/i18n/bundles.ts` schreiben**

```ts
import { de } from './de'
import type { Locale, Messages } from './messages'

/** EN/FR/ES/JA zeigen bis zu ihren Übersetzungs-Tasks auf die deutsche
 *  Referenz, damit die App in jeder Phase vollständig läuft. */
export const BUNDLES: Record<Locale, Messages> = {
  de,
  en: de,
  fr: de,
  es: de,
  ja: de,
}
```

- [ ] **Step 8: Failing Rauchtest für die Bundles schreiben**

`src/i18n/bundles.test.ts` — prüft Funktions-Messages jedes Bundles auf
nicht-leere Ergebnisse. Solange EN–JA auf `de` zeigen, testet er real nur
das de-Bundle; ab den Übersetzungs-Tasks greift er automatisch pro Sprache.
`locale` wird erst geprüft, wenn das Bundle nicht mehr der de-Alias ist:

```ts
import { describe, expect, it } from 'vitest'
import { BUNDLES } from './bundles'
import { de } from './de'
import { SUPPORTED_LOCALES } from './messages'

describe('bundles', () => {
  it.each(SUPPORTED_LOCALES)('%s: Funktions-Messages liefern nicht-leere Strings', (locale) => {
    const m = BUNDLES[locale]
    if (m !== de) expect(m.locale).toBe(locale)
    expect(m.app.loadError('HTTP 500')).toContain('HTTP 500')
    expect(m.upload.errTooLarge('60', '50').length).toBeGreaterThan(0)
    expect(m.upload.errTooMany('10.000').length).toBeGreaterThan(0)
    expect(m.report.readValue('1.334', '1.334', '935', 1988).length).toBeGreaterThan(0)
    expect(m.report.readValue('1.334', '1.334', '935', null).length).toBeGreaterThan(0)
    expect(m.report.readDaysValue(4, 20, 209).length).toBeGreaterThan(0)
    expect(m.filter.tag('Japan')).toContain('Japan')
    expect(m.filter.acquired(2010, 2015)).toContain('2010')
    expect(m.chips.removeAria('X').length).toBeGreaterThan(0)
    expect(m.views.shelf.decade(1990).length).toBeGreaterThan(0)
    expect(m.views.network.nodeTitle('Japan', '948')).toContain('Japan')
    expect(m.views.years.tooltip(1998, 2005, '3')).toContain('1998')
    expect(m.views.pace.dotTitle('T', '300', '4', true).length).toBeGreaterThan(0)
    expect(m.views.canon.counts('12', '5').length).toBeGreaterThan(0)
    expect(Object.keys(m.ddc.labels)).toHaveLength(10)
    expect(Object.keys(m.ddc.short)).toHaveLength(10)
  })
})
```

- [ ] **Step 9: Rauchtest laufen lassen**

Run: `npx vitest run src/i18n/bundles.test.ts`
Expected: PASS (5 Durchläufe, einer je Locale)

- [ ] **Step 10: `src/i18n/LocaleContext.tsx` schreiben**

```tsx
import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from 'react'
import { BUNDLES } from './bundles'
import { detectLocale } from './detect'
import type { Locale, Messages } from './messages'

const STORAGE_KEY = 'tsundoku.locale'

interface I18n {
  locale: Locale
  m: Messages
  /** Locale-gebundenes Zahlenformat; formatiert auch Dezimalzahlen (Rating). */
  fmtInt: (n: number) => string
  setLocale: (l: Locale) => void
}

const LocaleContext = createContext<I18n | null>(null)

function readStored(): string | null {
  // localStorage kann werfen (Safari Private Mode, abgeschaltete Cookies)
  try {
    return localStorage.getItem(STORAGE_KEY)
  } catch {
    return null
  }
}

export function LocaleProvider({ children }: { children: ReactNode }) {
  const [locale, setLocaleState] = useState<Locale>(() =>
    detectLocale(readStored(), navigator.languages ?? []),
  )

  useEffect(() => {
    document.documentElement.lang = locale
  }, [locale])

  const value = useMemo<I18n>(() => {
    const nf = new Intl.NumberFormat(locale)
    return {
      locale,
      m: BUNDLES[locale],
      fmtInt: (n) => nf.format(n),
      setLocale: (l) => {
        // Nur die explizite Wahl wird gespeichert; Auto-Detect bleibt flüchtig.
        try {
          localStorage.setItem(STORAGE_KEY, l)
        } catch {
          // Persistenz ist Komfort, keine Voraussetzung
        }
        setLocaleState(l)
      },
    }
  }, [locale])

  return <LocaleContext.Provider value={value}>{children}</LocaleContext.Provider>
}

export function useI18n(): I18n {
  const ctx = useContext(LocaleContext)
  if (!ctx) throw new Error('useI18n outside LocaleProvider')
  return ctx
}
```

- [ ] **Step 11: `src/main.tsx` — Provider einhängen**

`<App />` wird zu:

```tsx
import { LocaleProvider } from './i18n/LocaleContext'
// …
createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <LocaleProvider>
      <App />
    </LocaleProvider>
  </StrictMode>,
)
```

- [ ] **Step 12: Gesamtprüfung**

Run: `npx tsc --noEmit && npm test`
Expected: beides grün; die App selbst nutzt die Messages noch nicht (folgt in Tasks 3–5).

- [ ] **Step 13: Commit**

```bash
git add src/i18n src/main.tsx
git commit -m "feat(i18n): Messages-Interface, de-Bundle, Locale-Detection und Provider"
```

---

### Task 2: Sprachnamen über Intl.DisplayNames

**Files:**
- Modify: `src/lib/languages.ts`
- Test: `src/lib/languages.test.ts` (neu)

**Interfaces:**
- Consumes: `Messages`, `Locale` aus `src/i18n/messages.ts`; `de` aus `src/i18n/de`.
- Produces: `langLabel(code: string, m: Messages): string` — **neue Signatur** (bisher `langLabel(code)`). `LANG_ISO: Record<string, string>`, `OTHER_LANG`, `UNKNOWN_LANG`, `LANG_COLORS` unverändert exportiert. Die alte Tabelle `LANG_LABELS` entfällt.
- Achtung: Die Aufrufer von `langLabel` (Shelf, LanguageFlow, ReadingPace, BookDetail, filters.ts) werden erst in Tasks 3–5 umgestellt. Damit dieser Task eigenständig kompiliert, stellt er **alle bestehenden Aufrufer im selben Task mechanisch um**, siehe Step 4.

- [ ] **Step 1: Failing Test schreiben**

`src/lib/languages.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { de } from '../i18n/de'
import { langLabel } from './languages'

// ja-Bundle existiert erst ab Task 9; für den Locale-Wechsel reicht ein
// künstliches Messages-Objekt mit anderer locale.
const jaLike = { ...de, locale: 'ja' as const }

describe('langLabel', () => {
  it('zeigt Sprachen in der UI-Sprache an (Intl.DisplayNames)', () => {
    expect(langLabel('Japanese', de)).toBe('Japanisch')
    expect(langLabel('German', de)).toBe('Deutsch')
    expect(langLabel('German', jaLike)).toBe('ドイツ語')
    expect(langLabel('Greek (Ancient)', de)).toBe('Altgriechisch')
  })
  it('nutzt Message-Schlüssel für die Sentinel-Werte', () => {
    expect(langLabel('andere', de)).toBe(de.lang.other)
    expect(langLabel('unbekannt', de)).toBe(de.lang.unknown)
  })
  it('reicht unbekannte LibraryThing-Namen unverändert durch', () => {
    expect(langLabel('Klingon', de)).toBe('Klingon')
  })
})
```

- [ ] **Step 2: Test laufen lassen**

Run: `npx vitest run src/lib/languages.test.ts`
Expected: FAIL (langLabel hat noch die alte Signatur / LANG_LABELS-Logik)

- [ ] **Step 3: `src/lib/languages.ts` umbauen**

`LANG_LABELS` ersetzen durch (Dateikopf-Kommentar zur Datenherkunft der
englischen Namen beibehalten):

```ts
import type { Locale, Messages } from '../i18n/messages'

/** LibraryThing-Sprachname -> ISO-639-Code; Anzeige übernimmt Intl.DisplayNames. */
export const LANG_ISO: Record<string, string> = {
  German: 'de',
  English: 'en',
  Japanese: 'ja',
  Chinese: 'zh',
  Spanish: 'es',
  French: 'fr',
  Latin: 'la',
  Russian: 'ru',
  Italian: 'it',
  Dutch: 'nl',
  Polish: 'pl',
  Portuguese: 'pt',
  Hungarian: 'hu',
  Ukrainian: 'uk',
  Turkish: 'tr',
  Czech: 'cs',
  Korean: 'ko',
  'Greek (Ancient)': 'grc',
}

const displayNames = new Map<Locale, Intl.DisplayNames>()

export function langLabel(code: string, m: Messages): string {
  if (code === OTHER_LANG) return m.lang.other
  if (code === UNKNOWN_LANG) return m.lang.unknown
  const iso = LANG_ISO[code]
  if (!iso) return code
  let dn = displayNames.get(m.locale)
  if (!dn) {
    dn = new Intl.DisplayNames([m.locale], { type: 'language' })
    displayNames.set(m.locale, dn)
  }
  const label = dn.of(iso)
  // DisplayNames gibt bei unbekanntem Code die Eingabe zurück — dann ist der
  // rohe LibraryThing-Name die bessere Anzeige.
  return label && label !== iso ? label : code
}
```

`OTHER_LANG`, `UNKNOWN_LANG`, `LANG_COLORS` bleiben unverändert in der Datei.

- [ ] **Step 4: Bestehende Aufrufer mechanisch anpassen**

Damit `tsc` grün bleibt, in diesem Task rein mechanisch (die volle
i18n-Umstellung dieser Dateien folgt in Tasks 3–5): in
`src/store/filters.ts`, `src/components/BookDetail.tsx`,
`src/views/Shelf.tsx`, `src/views/LanguageFlow.tsx`,
`src/views/ReadingPace.tsx` jeweils `import { de } from '../i18n/de'`
(bzw. `'../../src…'`-relativ passend) ergänzen und jeden
`langLabel(x)`-Aufruf zu `langLabel(x, de)` machen. Das ist ein bewusster
Zwischenzustand: Task 3/5 ersetzen `de` durch das aktive Bundle aus `useI18n()`.

- [ ] **Step 5: Tests laufen lassen**

Run: `npx vitest run src/lib/languages.test.ts && npx tsc --noEmit && npm test`
Expected: alles PASS (auch `filters.test.ts`, das über `langLabel` deutsche Namen erwartet — de bleibt ja Referenz)

- [ ] **Step 6: Commit**

```bash
git add src/lib/languages.ts src/lib/languages.test.ts src/store/filters.ts src/components/BookDetail.tsx src/views/Shelf.tsx src/views/LanguageFlow.tsx src/views/ReadingPace.tsx
git commit -m "feat(i18n): Sprachnamen über Intl.DisplayNames statt handgepflegter Tabelle"
```

---

### Task 3: Shell, Filter und Kernkomponenten auf useI18n

**Files:**
- Modify: `src/App.tsx`, `src/store/filters.ts`, `src/components/FilterChips.tsx`, `src/components/EmptyState.tsx`, `src/components/DataSummary.tsx`, `src/components/CoverageNote.tsx`, `src/components/Footer.tsx`, `src/components/Footer.module.css`
- Test: `src/store/filters.test.ts` (anpassen)

**Interfaces:**
- Consumes: `useI18n()` aus Task 1; `langLabel(code, m)` aus Task 2.
- Produces:
  - `filterLabel(f: Filter, m: Messages): string` — **neue Signatur**, pur, ohne Modul-Importe von Labels.
  - `CoverageNote({ covered, total, unit?, children })` — `unit` jetzt optional `string`; ohne Angabe `m.coverage.unitTitles`. Views übergeben ggf. `unit={m.coverage.unitTags}`.
  - `VIEW_REGISTRY: Partial<Record<ViewId, ComponentType>>` — nur noch Komponenten, **kein `label`-Feld mehr**; die Navigation rendert `m.nav[id]`.
  - Footer enthält den Sprachumschalter (`<select>` mit `LOCALE_NAMES`).

- [ ] **Step 1: `filterLabel`-Tests auf neue Signatur umstellen (failing)**

In `src/store/filters.test.ts` den `filterLabel`-describe-Block ersetzen:

```ts
import { de } from '../i18n/de'
// …
describe('filterLabel', () => {
  it('baut deutsche Labels', () => {
    expect(filterLabel({ kind: 'tag', value: 'Japan' }, de)).toBe('Tag: Japan')
    expect(filterLabel({ kind: 'ddcTop', value: 8 }, de)).toBe('Wissensgebiet: Literatur')
    expect(filterLabel({ kind: 'acquiredYear', from: 2010, to: 2015 }, de)).toBe('Erworben: 2010–2015')
    expect(filterLabel({ kind: 'readStatus', value: 'unread' }, de)).toBe('Status: ungelesen')
    expect(filterLabel({ kind: 'language', value: 'Japanese' }, de)).toBe('Sprache: Japanisch')
  })
})
```

Run: `npx vitest run src/store/filters.test.ts` → FAIL (Signatur).

- [ ] **Step 2: `src/store/filters.ts` umbauen**

`MEDIA_LABELS`-Konstante und die Importe von `DDC_SHORT` entfernen;
`filterLabel` wird:

```ts
import type { Messages } from '../i18n/messages'
// …
export function filterLabel(f: Filter, m: Messages): string {
  switch (f.kind) {
    case 'tag':
      return m.filter.tag(f.value)
    case 'language':
      return m.filter.language(langLabel(f.value, m))
    case 'originalLanguage':
      return m.filter.originalLanguage(langLabel(f.value, m))
    case 'ddcTop':
      return m.filter.ddcTop(m.ddc.short[f.value] ?? String(f.value))
    case 'mediaType':
      return m.filter.mediaType(m.media[f.value])
    case 'collection':
      return m.filter.collection(f.value)
    case 'author':
      return m.filter.author(f.value)
    case 'award':
      return m.filter.award(f.value)
    case 'acquiredYear':
      return m.filter.acquired(f.from, f.to)
    case 'readYear':
      return m.filter.read(f.from, f.to)
    case 'editionYear':
      return m.filter.edition(f.from, f.to)
    case 'readStatus':
      return f.value === 'read' ? m.filter.statusRead : m.filter.statusUnread
  }
}
```

Der `import { de }`-Zwischenzustand aus Task 2 Step 4 entfällt damit in dieser Datei.

- [ ] **Step 3: `FilterChips`, `EmptyState`, `DataSummary`, `CoverageNote` umstellen**

Jeweils `const { m, fmtInt } = useI18n()` (wo Zahlen formatiert werden) und
alle deutschen Strings durch die Message-Schlüssel ersetzen:

- `FilterChips`: `aria-label={m.chips.regionAria}`, `filterLabel(f, m)`, `aria-label={m.chips.removeAria(label)}`, Button-Text `m.chips.clearAll`.
- `EmptyState`: `m.empty.title`, `m.empty.active`, `m.empty.release`, `m.chips.clearAll`; `filterLabel(f, m)`.
- `DataSummary`: Zellen `m.summary.titles` / `m.summary.read` / `m.summary.pages`; Hinweis `m.summary.filteredFrom(fmtInt(books.length))`; `fmtInt` aus `useI18n()` statt aus `../lib/format`.
- `CoverageNote`:

```tsx
export function CoverageNote({ covered, total, unit, children }: {
  covered: number
  total: number
  unit?: string
  children: ReactNode
}) {
  const { m, fmtInt } = useI18n()
  return (
    <p className={styles.note}>
      {m.coverage.frame(
        <span className={styles.num}>{fmtInt(covered)}</span>,
        <span className={styles.num}>{fmtInt(total)}</span>,
        unit ?? m.coverage.unitTitles,
      )}{' '}
      {children}
    </p>
  )
}
```

(`Num` bleibt unverändert exportiert.)

- [ ] **Step 4: `App.tsx` umstellen**

- `VIEW_REGISTRY` verliert das `label`-Feld: `Partial<Record<ViewId, ComponentType>>` mit `shelf: Shelf, …`; die Nav-Buttons rendern `{m.nav[id]}`.
- `Shell` und `App` holen `const { m } = useI18n()`.
- Texte: `m.app.loading`, `m.app.loadError(load.message)`, `m.app.incompatibleNotice` (ersetzt den Inline-String im `setLoad`), `m.app.replaceLibrary`, `nav aria-label={m.app.navAria}`.
- Achtung: `notice` wird als bereits übersetzter String an `DataUpload` gereicht — stattdessen jetzt einen Marker reichen: `notice`-Prop von `DataUpload` bleibt `string`; `App` übergibt `m.app.incompatibleNotice`. Da `m` im Effekt-Closure veraltet sein kann, den Notice-Zustand als Flag speichern (`state: 'missing'; incompatible: boolean`) und erst beim Rendern in `m.app.incompatibleNotice` auflösen — sonst bleibt der Text nach einem Sprachwechsel in der alten Sprache stehen.

- [ ] **Step 5: Footer mit Sprachumschalter**

```tsx
import { useI18n } from '../i18n/LocaleContext'
import { LOCALE_NAMES, SUPPORTED_LOCALES, type Locale } from '../i18n/messages'
// … im Footer:
const { locale, m, setLocale } = useI18n()
// nach dem letzten Link, mit Separator davor:
<span className={styles.sep} aria-hidden="true">·</span>
<select
  className={styles.lang}
  value={locale}
  aria-label={m.footer.languageAria}
  onChange={(e) => setLocale(e.target.value as Locale)}
>
  {SUPPORTED_LOCALES.map((l) => (
    <option key={l} value={l}>{LOCALE_NAMES[l]}</option>
  ))}
</select>
```

Linktexte: `m.footer.license`, `m.footer.embedded`. In `Footer.module.css`
eine schlichte `.lang`-Klasse ergänzen (Schrift erben, Hintergrund
transparent, Rahmen dezent wie die Links):

```css
.lang {
  font: inherit;
  font-size: 13px;
  color: inherit;
  background: transparent;
  border: 1px solid var(--ink-15);
  border-radius: var(--radius);
  padding: 2px 4px;
}
```

- [ ] **Step 6: Prüfen**

Run: `npx tsc --noEmit && npm test`
Expected: grün. Dev-Server-Stichprobe: `npm run dev` — Navigation, Chips, Footer-Umschalter sichtbar; Umschalten ändert Chips/Nav noch nicht überall (Views folgen), aber Shell-Texte wechseln.

- [ ] **Step 7: Commit**

```bash
git add src/App.tsx src/store/filters.ts src/store/filters.test.ts src/components/FilterChips.tsx src/components/EmptyState.tsx src/components/DataSummary.tsx src/components/CoverageNote.tsx src/components/Footer.tsx src/components/Footer.module.css
git commit -m "feat(i18n): Shell, Filter-Labels und Kernkomponenten über Messages"
```

---

### Task 4: DataUpload und BookDetail auf useI18n

**Files:**
- Modify: `src/components/DataUpload.tsx`, `src/components/BookDetail.tsx`

**Interfaces:**
- Consumes: `useI18n()`, Message-Schlüssel `upload.*`, `report.*`, `media`, `detail.*`; `langLabel(code, m)`.
- Produces: keine neuen Schnittstellen; `DataUpload`-Props unverändert.

- [ ] **Step 1: `DataUpload.tsx` umstellen**

`const { m, fmtInt } = useI18n()`; `fmtInt`-Import aus `../lib/format` entfernen. Ersetzungen:

- Fehlerpfade in `handleFile`: Die geworfenen Fehlermeldungen durch die
  Message-Funktionen ersetzen — `m.upload.errTooLarge(fmtInt(Math.round(file.size / 1e6)), fmtInt(MAX_RAW_BYTES / 1e6))`,
  `m.upload.errNotAnExport`, `m.upload.errTooMany(fmtInt(MAX_BOOKS))`,
  `m.upload.errNoBooks`, SyntaxError-Fall `m.upload.errInvalidJson`.
- Report-Zeilen (`rows`) mit den `report.*`-Schlüsseln:

```tsx
const rows: [string, string][] = [
  [m.report.entries, fmtInt(stats.total)],
  [m.report.media, stats.byMediaType.map(([t, n]) => `${m.media[t as MediaType]} ${fmtInt(n)}`).join(', ')],
  [m.report.read, m.report.readValue(
    fmtInt(stats.read),
    fmtInt(stats.withReadYearEffective),
    fmtInt(stats.withReadDate),
    readYears.length ? Math.min(...readYears) : null,
  )],
  [m.report.pagesTotal, fmtInt(stats.pagesTotal)],
  ...(stats.readDays.median !== null
    ? ([[m.report.readDays, m.report.readDaysValue(stats.readDays.median, stats.readDays.p90!, stats.readDays.max!)]] as [string, string][])
    : []),
  [m.report.tags, m.report.tagsValue(fmtInt(stats.tagsNorm.length), fmtInt(rawTagCount))],
  [m.report.dimsSwapped, m.report.dimsSwappedValue(fmtInt(stats.dimsSorted), fmtInt(stats.dimsDiscarded))],
  [m.report.dimsEstimated, m.report.dimsEstimatedValue(fmtInt(stats.dimsEstimated))],
  [m.report.origLangInferred, m.report.origLangInferredValue(fmtInt(stats.origLangInferred))],
  [m.report.entitiesDecoded, m.report.entitiesDecodedValue(fmtInt(stats.entitiesDecoded))],
  [m.report.bulkImport, m.report.bulkImportValue(fmtInt(stats.bulkImported))],
]
```

- Übrige Texte: `m.report.title`, `m.report.note`, `m.report.toLibrary`,
  `m.report.otherFile`, `m.upload.intro`, `m.upload.title` (h2),
  `m.upload.ltIntro`, `m.upload.working`, `m.upload.dropHere`,
  `m.upload.chooseFile`, Fehleranzeige `{m.upload.errorPrefix} {up.message}`,
  `m.upload.backToLoaded`.
- Wichtig: In den State wandern **keine fertig übersetzten Strings**, außer
  der Fehlermeldung (`up.message`) — die entsteht beim Wurf. Das ist ein
  akzeptierter Kompromiss: Fehlertext wechselt erst beim nächsten Versuch
  die Sprache; als Kommentar an der Stelle dokumentieren.

- [ ] **Step 2: `BookDetail.tsx` umstellen**

`const { m, fmtInt } = useI18n()`; Ersetzungen:

- Zeilen-Labels: `m.detail.original`, `m.detail.editionYear`, `m.detail.language`, `m.detail.originalLanguage`, `m.detail.pages`, `m.detail.ddc`, `m.detail.acquired`, `m.detail.read`, `m.detail.rating`, `m.detail.boughtAt`, `m.detail.series`, `m.detail.isbn`, `m.detail.tags`.
- Werte: `langLabel(l, m)`; Lesejahr aus Jahres-Tag `m.detail.readTagged(book.readYearEffective)`; Rating `` `★ ${fmtInt(book.rating)}` `` statt `toLocaleString('de-DE')`.
- **DDC-Chip:** statt `book.ddc.topLabel` (deutsches Label aus dem Normalisierungslauf, in den Daten eingebrannt) jetzt `m.ddc.labels[book.ddc.top]` anzeigen.
- Aria: `m.detail.toggleFilterAria(filterLabel(f, m))`, `m.detail.filterByAuthorAria(a.name)`.
- `m.detail.viewOnLt`, `m.detail.close`.
- Der `import { de }`-Zwischenzustand aus Task 2 entfällt.

- [ ] **Step 3: Prüfen**

Run: `npx tsc --noEmit && npm test`
Expected: grün. Dev-Stichprobe: Upload-Seite (library.json wegbenennen oder `?`-losen Pfad prüfen) zeigt alle Texte; Detailkarte im Regal öffnen.

- [ ] **Step 4: Commit**

```bash
git add src/components/DataUpload.tsx src/components/BookDetail.tsx
git commit -m "feat(i18n): Upload-Seite und Buch-Detailkarte über Messages"
```

---

### Task 5: Die acht Views auf useI18n

**Files:**
- Modify: `src/views/Shelf.tsx`, `src/views/AcquisitionReading.tsx`, `src/views/KnowledgeMap.tsx`, `src/views/TagNetwork.tsx`, `src/views/LanguageFlow.tsx`, `src/views/YearMatrix.tsx`, `src/views/ReadingPace.tsx`, `src/views/CanonCheck.tsx`, `src/lib/format.ts`, `src/lib/format.test.ts`, `src/lib/ddc.ts`

**Interfaces:**
- Consumes: `useI18n()`; `m.views.*`, `m.rangeForm.*`, `m.coverage.unitTags`, `m.ddc.labels/short`, `langLabel(code, m)`.
- Produces: `src/lib/format.ts` exportiert danach **nur noch `fmtYear`** — `fmtInt` existiert ausschließlich in `useI18n()`. `src/lib/ddc.ts` exportiert nur noch `DDC_COLORS` (Labels leben in den Bundles).

Pro View: `const { m, fmtInt } = useI18n()`, `fmtInt`-Import entfernen, alle
deutschen Strings gegen die in Task 1 definierten Schlüssel tauschen. Die
Zuordnung String → Schlüssel ergibt sich eindeutig aus `de.tsx` (Task 1
Step 4) — dort steht jeder heutige Text wörtlich. Besonderheiten:

- **Shelf:** `SORT_LABELS`/`COLOR_LABELS`-Konstanten entfernen, stattdessen `m.views.shelf.sortLabels`/`colorLabels` (die `<option>`-Schleifen iterieren über `Object.entries(m.views.shelf.sortLabels)`). `buildLegend` bekommt `m` als Parameter (`buildLegend(color, books, yearScale, m)`) für `noInfo`, `legendRead`, `legendUnread`, `noAcqYear`, `decade` und `langLabel(x, m)` sowie `m.ddc.short`. Tooltip-Zusatz `m.views.shelf.estimatedShort`, `spineLabel` nutzt `m.views.shelf.estimatedSuffix`.
- **AcquisitionReading & LanguageFlow:** das von/bis-Formular nutzt die gemeinsamen `m.rangeForm.*`-Schlüssel (`dimensionAria`, Optionen `acquired`/`read`, `from`, `to`, `submit`).
- **AcquisitionReading:** Tooltip `"<strong>{year}</strong>: " + m.views.timeline.tooltipAcquired(fmtInt(n))` — das Jahr bleibt im JSX, nur der Zählsatz ist Message.
- **KnowledgeMap:** Stream-`<title>` über `m.views.knowledge.streamTitle(s.key, m.ddc.labels[s.key], fmtInt(…))`; Legende `m.ddc.labels[c]` statt `DDC_LABELS[c]`.
- **TagNetwork:** Coverage-Funktion erhält `minCount` als `number` (erster Parameter), die drei Zählwerte als `<Num>`-Knoten; `unit={m.coverage.unitTags}`.
- **YearMatrix:** eigene `m.views.years.submit` („Bereich filtern") statt `rangeForm.submit`.
- **ReadingPace:** `m.views.pace.dotTitle(title, pagesFmt, daysFmt, p.suspect)`; Achsen `axisPages`/`axisDays`; `figure key={panel.lang ?? 'alle'}` — der Key ist kein UI-Text, bleibt.
- **CanonCheck:** `m.views.canon.*`.
- **`src/lib/ddc.ts`:** `DDC_LABELS` und `DDC_SHORT` löschen (nach Umstellung aller Verweise auf `m.ddc.*`); `DDC_COLORS` bleibt.
- **`src/lib/format.ts`:** `fmtInt` und den `Intl.NumberFormat('de-DE')` löschen; `fmtYear` bleibt. `format.test.ts` auf `fmtYear` reduzieren:

```ts
import { expect, it } from 'vitest'
import { fmtYear } from './format'

it('formatiert Jahre ohne Tausendertrennung', () => {
  expect(fmtYear(1998)).toBe('1998')
})
```

- [ ] **Step 1: Views umstellen** (Datei für Datei, nach jeder Datei `npx tsc --noEmit`)
- [ ] **Step 2: `ddc.ts` und `format.ts` bereinigen** — `tsc` findet vergessene Verweise
- [ ] **Step 3: Prüfen**

Run: `npx tsc --noEmit && npm test`
Expected: grün; `grep -rn "from '../lib/format'" src/views src/components | grep fmtInt` liefert nichts mehr.

- [ ] **Step 4: Dev-Stichprobe**

`npm run dev`, alle acht Views durchklicken (Sprache Deutsch): identisches Erscheinungsbild wie vor der Umstellung.

- [ ] **Step 5: Commit**

```bash
git add src/views src/lib/format.ts src/lib/format.test.ts src/lib/ddc.ts
git commit -m "feat(i18n): alle acht Views über Messages, fmtInt nur noch locale-gebunden"
```

---

### Task 6: Englische Übersetzung (`en.tsx`)

**Files:**
- Create: `src/i18n/en.tsx`
- Modify: `src/i18n/bundles.ts` (`en: de` → `en` importieren)

**Interfaces:**
- Consumes: `Messages` aus `./messages`; `de.tsx` als inhaltliche Referenz.
- Produces: `export const en: Messages` mit `locale: 'en'`.

**Übersetzungs-Brief (gilt wörtlich; Ausführung auf dem fähigsten verfügbaren Modell):**

> Übersetze das komplette deutsche Message-Bundle `src/i18n/de.tsx` nach
> **Englisch** als `src/i18n/en.tsx` (`export const en: Messages`,
> `locale: 'en'`). Regeln:
> - **Register:** neutral-direkt, zweite Person („your library"), kein
>   Marketing-Ton. Die App ist ein kontemplatives Datenporträt einer
>   Privatbibliothek, keine SaaS-Landingpage.
> - **Funktions-Messages frei formulieren** — natürliche englische Syntax,
>   nicht Wort-für-Wort. Zahlen kommen vorformatiert als String (`…Fmt`),
>   Jahreszahlen als `number`.
> - **Fachbegriffe:** Dewey-Klassen mit den englischen DDC-Standardnamen
>   (000 „General works & computer science" → kurz „Computer science"; 300
>   „Social sciences"; 800 „Literature" usw.). „Wissensgebiet" → „Subject
>   area". „Erwerb" → „acquisition/acquired", „Lektüre/gelesen" →
>   „reading/read". „Jahres-Tag" (Tag wie `2005` als Lesejahr-Beleg) →
>   „year tag". „Massenimport" → „bulk import". Medientyp „Schallplatte" →
>   „Vinyl".
> - **View-Namen:** Regal → „The Shelf" (nav kurz „Shelf"), Erwerb &
>   Lektüre → „Acquisitions & Reading", Wissenslandkarte → „Knowledge
>   Map", Tag-Netzwerk → „Tag Network", Sprachfluss → „Language Flow",
>   Ausgabe × Erwerb → „Edition × Acquisition", Lesetempo → „Reading
>   Pace", Kanon → „Canon".
> - **積ん読 erklären** wie im deutschen Intro: “buying books and letting
>   them pile up, unread.”
> - **Typografie:** englische Anführungszeichen (“…”), Gedankenstrich mit
>   em-dash ohne Leerzeichen oder en-dash mit — konsistent bleiben.
> - **JSX-Struktur spiegeln:** gleiche `<em>`/`<strong>`/`<a>`-Elemente,
>   identische URLs und Link-Ziele wie in `de.tsx`.
> - **Nicht übersetzen:** `locale`-Feld-Wert, Endonyme, Platzhalter-Werte,
>   „LibraryThing", „Tsundoku 積ん読", „ISBN", „MIT".

- [ ] **Step 1: `en.tsx` schreiben** (vollständig, jeder Schlüssel)
- [ ] **Step 2: `bundles.ts` aktualisieren** (`import { en } from './en'`, Eintrag `en`)
- [ ] **Step 3: Prüfen**

Run: `npx tsc --noEmit && npx vitest run src/i18n/bundles.test.ts`
Expected: grün — der Rauchtest prüft jetzt für `en` auch `m.locale === 'en'`.

- [ ] **Step 4: Commit**

```bash
git add src/i18n/en.tsx src/i18n/bundles.ts
git commit -m "feat(i18n): englisches Message-Bundle"
```

---

### Task 7: Französische Übersetzung (`fr.tsx`)

**Files:**
- Create: `src/i18n/fr.tsx`
- Modify: `src/i18n/bundles.ts`

**Interfaces:** wie Task 6, mit `export const fr: Messages`, `locale: 'fr'`.

**Übersetzungs-Brief (gilt wörtlich; Ausführung auf dem fähigsten verfügbaren Modell):**

> Übersetze das komplette deutsche Message-Bundle `src/i18n/de.tsx` nach
> **Französisch** als `src/i18n/fr.tsx` (`export const fr: Messages`,
> `locale: 'fr'`). Regeln:
> - **Register: Vouvoiement** („votre bibliothèque") — die französische
>   UI-Konvention; nüchtern-elegant, kein Marketing-Ton.
> - **Funktions-Messages frei formulieren** — natürliche französische
>   Syntax mit korrekten Kontraktionen und Genus-Kongruenz. Zahlen kommen
>   vorformatiert als String (`…Fmt`), Jahreszahlen als `number`.
> - **Typografie:** französische Anführungszeichen « … » mit schmalem
>   Leerzeichen, Leerzeichen vor Doppelpunkt/Prozent gemäß französischer
>   Norm (in Labels wie „Sprache: X" also « Langue : X »).
> - **Fachbegriffe:** Dewey-Klassen mit den französischen
>   CDD-Standardnamen (300 « Sciences sociales », 800 « Littérature »
>   usw.). „Wissensgebiet" → « Domaine ». „Jahres-Tag" → « tag d'année ».
>   „Massenimport" → « import en masse ». „Schallplatte" → « Vinyle ».
> - **View-Namen:** Regal → « L'Étagère » (nav « Étagère »), Erwerb &
>   Lektüre → « Acquisitions & lectures », Wissenslandkarte → « Carte des
>   savoirs », Tag-Netzwerk → « Réseau de tags », Sprachfluss → « Flux des
>   langues », Ausgabe × Erwerb → « Édition × acquisition », Lesetempo →
>   « Rythme de lecture », Kanon → « Canon ».
> - **積ん読 erklären** wie im deutschen Intro (acheter des livres et les
>   empiler sans les lire).
> - **JSX-Struktur spiegeln:** gleiche `<em>`/`<strong>`/`<a>`-Elemente,
>   identische URLs wie in `de.tsx`.
> - **Nicht übersetzen:** `locale`-Wert, Endonyme, „LibraryThing",
>   „Tsundoku 積ん読", „ISBN", „MIT".

- [ ] **Step 1: `fr.tsx` schreiben**
- [ ] **Step 2: `bundles.ts` aktualisieren**
- [ ] **Step 3: Prüfen** — `npx tsc --noEmit && npx vitest run src/i18n/bundles.test.ts` → grün
- [ ] **Step 4: Commit** — `git add src/i18n/fr.tsx src/i18n/bundles.ts && git commit -m "feat(i18n): französisches Message-Bundle"`

---

### Task 8: Spanische Übersetzung (`es.tsx`)

**Files:**
- Create: `src/i18n/es.tsx`
- Modify: `src/i18n/bundles.ts`

**Interfaces:** wie Task 6, mit `export const es: Messages`, `locale: 'es'`.

**Übersetzungs-Brief (gilt wörtlich; Ausführung auf dem fähigsten verfügbaren Modell):**

> Übersetze das komplette deutsche Message-Bundle `src/i18n/de.tsx` nach
> **Spanisch** als `src/i18n/es.tsx` (`export const es: Messages`,
> `locale: 'es'`). Regeln:
> - **Register: Tuteo** („tu biblioteca") — übliche UI-Praxis; warm, aber
>   nüchtern, kein Marketing-Ton.
> - **Funktions-Messages frei formulieren** — natürliche spanische Syntax.
>   Zahlen kommen vorformatiert als String (`…Fmt`), Jahre als `number`.
> - **Typografie:** spanische Anführungszeichen « » oder „…" konsistent
>   (empfohlen « »), keine invertierten Fragezeichen nötig (keine Fragen
>   im Bundle — falls doch formuliert, korrekt mit ¿…?).
> - **Fachbegriffe:** Dewey-Klassen mit den spanischen CDD-Standardnamen
>   (300 « Ciencias sociales », 800 « Literatura » usw.). „Wissensgebiet"
>   → « Materia ». „Jahres-Tag" → « etiqueta de año ». „Massenimport" →
>   « importación masiva ». „Schallplatte" → « Vinilo ».
> - **View-Namen:** Regal → « La Estantería » (nav « Estantería »),
>   Erwerb & Lektüre → « Adquisición y lectura », Wissenslandkarte →
>   « Mapa del conocimiento », Tag-Netzwerk → « Red de etiquetas »,
>   Sprachfluss → « Flujo de idiomas », Ausgabe × Erwerb → « Edición ×
>   adquisición », Lesetempo → « Ritmo de lectura », Kanon → « Canon ».
>   Achtung: „Tag" heißt in der Datenwelt der App „etiqueta", aber
>   Datenwerte (die Tags selbst) bleiben unübersetzt.
> - **積ん読 erklären** wie im deutschen Intro (comprar libros y dejarlos
>   apilarse sin leerlos).
> - **JSX-Struktur spiegeln:** gleiche Elemente und URLs wie in `de.tsx`.
> - **Nicht übersetzen:** `locale`-Wert, Endonyme, „LibraryThing",
>   „Tsundoku 積ん読", „ISBN", „MIT".

- [ ] **Step 1: `es.tsx` schreiben**
- [ ] **Step 2: `bundles.ts` aktualisieren**
- [ ] **Step 3: Prüfen** — `npx tsc --noEmit && npx vitest run src/i18n/bundles.test.ts` → grün
- [ ] **Step 4: Commit** — `git add src/i18n/es.tsx src/i18n/bundles.ts && git commit -m "feat(i18n): spanisches Message-Bundle"`

---

### Task 9: Japanische Übersetzung (`ja.tsx`)

**Files:**
- Create: `src/i18n/ja.tsx`
- Modify: `src/i18n/bundles.ts`

**Interfaces:** wie Task 6, mit `export const ja: Messages`, `locale: 'ja'`.

**Übersetzungs-Brief (gilt wörtlich; Ausführung auf dem fähigsten verfügbaren Modell):**

> Übersetze das komplette deutsche Message-Bundle `src/i18n/de.tsx` nach
> **Japanisch** als `src/i18n/ja.tsx` (`export const ja: Messages`,
> `locale: 'ja'`). Regeln:
> - **Register: höfliche です/ます-Form**, durchgängig; Beschriftungen
>   (Labels, Buttons, Achsen) im üblichen knappen UI-Stil ohne Kopula
>   (z. B. 「並び順」, 「絞り込み解除」), Fließtexte höflich.
> - **積ん読 NICHT erklären** — das Publikum kennt den Begriff. Das Intro
>   spielt stattdessen mit ihm: Die App macht das eigene 積ん読 sichtbar —
>   was gekauft und gestapelt, was wirklich gelesen wurde. Sinngemäß neu
>   dichten, nicht übersetzen.
> - **Kein Plural, keine mechanischen Übernahmen:** Funktions-Messages
>   frei und natürlich formulieren (z. B. `counts` →
>   「所蔵 ${ownedFmt} 冊 · 読了 ${readFmt} 冊」). Zähleinheiten korrekt:
>   Bücher/Titel 冊 oder 点 (Medienmix: 点), Felder 件, Einträge 件,
>   Tage 日, Seiten ページ.
> - **Zahlen** kommen vorformatiert als String (`…Fmt`), Jahre als
>   `number` (Anzeige westlich: 「1998年」).
> - **Fachbegriffe bibliotheksnah:** Dewey-Hauptklassen in der in
>   japanischen Bibliotheken üblichen Terminologie (000 総記・情報学,
>   100 哲学・心理学, 200 宗教, 300 社会科学, 400 言語, 500 自然科学,
>   600 技術・医学, 700 芸術・娯楽, 800 文学, 900 歴史・地理 — Kurzformen
>   entsprechend). „Wissensgebiet" → 分野. „Erwerb" → 入手 (oder 購入,
>   konsistent wählen), „Lektüre/gelesen" → 読書/読了. „Jahres-Tag" →
>   年タグ. „Massenimport" → 一括インポート. „Schallplatte" → レコード.
>   „Sammlung" (LibraryThing collection) → コレクション.
> - **View-Namen:** Regal → 「本棚」, Erwerb & Lektüre → 「入手と読書」,
>   Wissenslandkarte → 「知の地図」, Tag-Netzwerk → 「タグネットワーク」,
>   Sprachfluss → 「言語の流れ」, Ausgabe × Erwerb → 「出版年×入手年」,
>   Lesetempo → 「読書ペース」, Kanon → 「正典リスト」.
> - **Typografie:** japanische Interpunktion (、。「」), keine
>   Leerzeichen zwischen japanischen Zeichen und Interpunktion; zwischen
>   Zahl+lateinischer Einheit und japanischem Text ein Halbbreit-Leerzeichen
>   nur, wo es die Lesbarkeit verlangt.
> - **JSX-Struktur spiegeln:** gleiche `<em>`/`<strong>`/`<a>`-Elemente
>   und URLs wie in `de.tsx` (em/strong tragen im Japanischen die gleiche
>   Auszeichnung, das CSS regelt die Darstellung).
> - **Nicht übersetzen:** `locale`-Wert, Endonyme, „LibraryThing",
>   „Tsundoku 積ん読" (Brand), „ISBN", „MIT", „DDC" (als Sigle ok neben
>   der japanischen Klassenbezeichnung).

- [ ] **Step 1: `ja.tsx` schreiben**
- [ ] **Step 2: `bundles.ts` aktualisieren**
- [ ] **Step 3: Prüfen** — `npx tsc --noEmit && npx vitest run src/i18n/bundles.test.ts` → grün
- [ ] **Step 4: Commit** — `git add src/i18n/ja.tsx src/i18n/bundles.ts && git commit -m "feat(i18n): japanisches Message-Bundle"`

---

### Task 10: Review der japanischen Fassung

**Kein Implementierungs-Task** — der Orchestrator dispatcht einen separaten
Review-Agenten (fähigstes Modell) mit diesem Auftrag:

> Prüfe `src/i18n/ja.tsx` gegen `src/i18n/de.tsx` als japanische
> UI-Lokalisierung: (1) Register — durchgängig です/ます in Fließtexten,
> knapper Label-Stil ohne Kopula in Buttons/Beschriftungen, keine
> Stilbrüche; (2) Natürlichkeit — liest sich jede Formulierung wie eine
> native japanische Anwendung, keine Übersetzungssyntax (翻訳調)?
> (3) Bibliotheksterminologie — Dewey-Klassennamen, 冊/点/件-Zähler,
> 読了/入手-Vokabular konsistent; (4) das Intro spielt mit 積ん読, statt
> es zu erklären; (5) Interpunktion 、。「」 korrekt. Melde konkrete
> Korrekturen als Diff-Vorschläge (Schlüssel + alt + neu + Begründung),
> keine Pauschalurteile.

Gefundene Korrekturen werden als Fix-Runde auf Task 9 angewendet
(`git commit -m "fix(i18n): JA-Review eingearbeitet"`).

---

### Task 11: Japanische Typografie (Shippori Mincho, Noto Serif JP, :lang(ja)-Tokens)

**Files:**
- Modify: `package.json` (2 neue Dependencies), `src/main.tsx` (Font-Importe), `src/styles/tokens.css`, `src/styles/global.css`, `src/components/BookDetail.module.css`, `src/App.module.css`

**Interfaces:**
- Consumes: `document.documentElement.lang` wird seit Task 1 vom Provider gesetzt — `:lang(ja)` greift damit automatisch.
- Produces: Tokens `--font-title`, `--line-height`, `--weight-display`; keine TS-Schnittstellen.

- [ ] **Step 1: Fonts installieren**

```bash
npm install @fontsource/shippori-mincho @fontsource/noto-serif-jp
```

- [ ] **Step 2: Importe in `src/main.tsx`** (zu den bestehenden Font-Importen)

```ts
import '@fontsource/shippori-mincho/500.css'
import '@fontsource/shippori-mincho/600.css'
import '@fontsource/noto-serif-jp/500.css'
```

(500 für den Namenszug „積ん読" in allen Sprachen und Buchtitel; 600 für
japanische Überschriften, die einen Schnitt schwerer laufen als Fraunces 500.
Fontsource-JP-Pakete sind per `unicode-range` in Subsets zerlegt — der
Browser lädt nur benötigte Slices.)

- [ ] **Step 3: `src/styles/tokens.css` erweitern**

Font-Stacks und neue Tokens (bestehende Zeilen ersetzen bzw. ergänzen):

```css
  --font-display: 'Fraunces', 'Shippori Mincho', 'Noto Sans JP', serif;
  --font-title: 'Fraunces', 'Noto Serif JP', serif;
  --font-body: 'Source Sans 3', 'Noto Sans JP', sans-serif;
  --font-mono: 'IBM Plex Mono', 'Noto Sans JP', monospace;
  --line-height: 1.5;
  --weight-display: 500;
```

Und ans Dateiende der neue Block:

```css
/* Optische Angleichung für japanische UI: Kanji füllen das Geviert (latein.
 * Versalhöhe ~0,7 em) -> Grundgröße leicht runter; Mincho-Waagerechte sind
 * dünn -> Display einen Schnitt schwerer; CJK braucht mehr Durchschuss. */
:root:lang(ja) {
  font-size: 15px; /* ~0,94 × 16px */
  --line-height: 1.85;
  --weight-display: 600;
}
```

- [ ] **Step 4: `src/styles/global.css` auf die Tokens umstellen**

```css
body {
  /* … */
  font-size: 1rem;          /* statt 16px — folgt der :lang(ja)-Root-Größe */
  line-height: var(--line-height);
}

h1, h2, h3 {
  font-family: var(--font-display);
  font-weight: var(--weight-display);
  margin: 0;
}
```

- [ ] **Step 5: Buchtitel auf `--font-title`**

In `src/components/BookDetail.module.css` der `.title`-Regel
`font-family: var(--font-title);` hinzufügen (Buchtitel sind Daten — der
Noto-Serif-JP-Fallback mit vollem Zeichenvorrat gilt in jeder UI-Sprache;
Shippori Mincho hat einen begrenzten Vorrat, ein Fallback-Bruch mitten im
Titel wäre sichtbar). Prüfen, ob weitere Stellen Buchtitel in
`--font-display` setzen: `grep -rn 'font-display' src --include='*.css'` —
Stand heute nur `global.css` (h1–h3) und ggf. `App.module.css` (.brand);
View-Überschriften (h2) sind UI-Text, sie bleiben Display/Shippori.

- [ ] **Step 6: Namenszug prüfen**

Der `<span lang="ja">積ん読</span>` im Header-h1 erbt `--font-display` —
durch die neue Stack-Reihenfolge rendert er jetzt in Shippori Mincho
(Fraunces hat keine CJK-Glyphen). Kein Codeeintrag nötig; nur verifizieren.

- [ ] **Step 7: Prüfen**

Run: `npm run build && npx tsc --noEmit && npm test`
Expected: grün; `ls dist/assets | grep -i shippori | head` zeigt gebündelte Subsets.
Dev-Stichprobe: Sprache auf 日本語 schalten — Überschriften in Mincho, engere Grundgröße, luftigere Zeilen; Detailkarte mit japanischem Buchtitel öffnen (z. B. im Regal nach einem japanischen Titel suchen): Titel in Noto Serif JP ohne Glyphen-Bruch.

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json src/main.tsx src/styles/tokens.css src/styles/global.css src/components/BookDetail.module.css
git commit -m "feat(i18n): japanische Typografie — Shippori Mincho, Noto Serif JP, :lang(ja)-Tokens"
```

---

### Task 12: Englische README, Lizenztabelle, CLAUDE.md-Konvention

**Files:**
- Modify: `README.md`, `CLAUDE.md`

**Brief für die README (Ausführung auf dem fähigsten verfügbaren Modell):**

> Schreibe `README.md` vollständig auf **Englisch** neu — gleiche
> Abschnittsstruktur und derselbe Inhalt wie die heutige deutsche Fassung
> (Intro mit 積ん読-Erklärung und der Leitfrage; „Was es zu sehen gibt" →
> "What you'll see" mit denselben vier Screenshots und View-Beschreibungen;
> Start/Local mit Node ≥ 24; Stack; Documents; Status; License). Zusätzlich:
> - Im Intro oder Start-Abschnitt **ein** Satz: die UI ist in fünf Sprachen
>   verfügbar (Deutsch, Englisch, Französisch, Spanisch, Japanisch),
>   automatisch erkannt, umschaltbar in der Fußzeile.
> - Screenshot-Pfade unverändert (`docs/screenshots/*.png`) — die Bilder
>   werden in Task 13 durch englische UI-Aufnahmen ersetzt.
> - Der Verweis, dass `docs/*.md` (Datenprofil, Visualisierungen) bewusst
>   deutsch bleiben, gehört in den Documents-Abschnitt.
> - **Lizenztabelle** übernehmen und um zwei Zeilen ergänzen:
>   `[Shippori Mincho](https://github.com/fontdasu/ShipporiMincho) © FONTDASU · Display-CJK (japanische UI, Namenszug) · OFL-1.1`
>   und
>   `[Noto Serif JP](https://fonts.google.com/noto/specimen/Noto+Serif+JP) © Google · Buchtitel-CJK-Fallback · OFL-1.1`
>   (Spaltenüberschriften der Tabelle auf Englisch: Component / Purpose /
>   License; Zweck-Spalte aller Zeilen mitübersetzen).
> - Ton: nüchtern, präzise, wie die deutsche Fassung; keine
>   Marketing-Floskeln. Der Anker `#lizenz`, auf den die Fußzeile verweist,
>   ändert sich durch die englische Überschrift („## License") zu
>   `#license` — **deshalb in `src/components/Footer.tsx` den Link
>   `${REPO}#lizenz` auf `${REPO}#license` ändern** (gehört zu diesem Task).

In `CLAUDE.md` zwei Anpassungen:
- Konventionszeile „**Deutsch in der Oberfläche**, Englisch im Code" ersetzen durch: „**Fünfsprachige Oberfläche** (DE/EN/FR/ES/JA) über typisierte Message-Bundles in `src/i18n/` — UI-Texte nie hart in Komponenten, `de.tsx` ist die Referenzfassung; Englisch im Code".
- Im Struktur-Block eine Zeile ergänzen: `src/i18n/                Messages-Interface + fünf Sprach-Bundles, LocaleContext (useI18n)`.

- [ ] **Step 1: README neu schreiben, Footer-Anker anpassen, CLAUDE.md ergänzen**
- [ ] **Step 2: Prüfen** — Links/Anker stichprobenartig, `npx tsc --noEmit && npm test` grün
- [ ] **Step 3: Commit**

```bash
git add README.md CLAUDE.md src/components/Footer.tsx
git commit -m "docs: englische README mit i18n-Abschnitt und erweiterter Lizenztabelle"
```

---

### Task 13: Screenshots (englische UI) und visuelle JA-Verifikation

**Kein Subagent-Task** — der Orchestrator führt ihn selbst mit dem
Playwright-MCP aus (eigener Dev-Server auf Port 5173, Bibliothek aus
`public/data/hidden/library.json` bzw. per Upload laden):

- [ ] **Step 1:** Sprache über den Fußzeilen-Umschalter auf **English** stellen; die vier README-Screenshots neu aufnehmen und ersetzen (gleiche Dateinamen, gleiche Filterzustände wie in den Bildunterschriften der README):
  - `docs/screenshots/begruessung-upload.png` — Begrüßungs-/Upload-Seite (dafür Zustand ohne geladene Bibliothek)
  - `docs/screenshots/regal-gefiltert.png` — Regal mit Filtern „Language: German" + „Acquired: 2020–2026"
  - `docs/screenshots/wissenslandkarte.png` — Knowledge Map ungefiltert
  - `docs/screenshots/sprachfluss-gefiltert.png` — Language Flow mit „Acquired: 2020–2026"
- [ ] **Step 2:** Sprache auf **日本語** stellen und visuell verifizieren (Screenshots zur Durchsicht, nicht fürs Repo): Header-Namenszug und Überschriften in Shippori Mincho, Zeilenhöhen ohne Überlappung, Trunkierungen intakt, Detailkarte mit japanischem Buchtitel in Noto Serif JP, Fußzeile und alle acht Views ohne Layoutbrüche; ferner FR (längste Strings — Umbrüche in Chips/Buttons prüfen).
- [ ] **Step 3:** Alle fünf Sprachen einmal durchschalten: Persistenz (Reload behält explizite Wahl), `document.documentElement.lang` wechselt, Zahlformate wechseln (de 1.334 / en 1,334 / fr 1 334 / ja 1,334).
- [ ] **Step 4: Commit**

```bash
git add docs/screenshots
git commit -m "docs: Screenshots mit englischer UI"
```

---

## Ausführungshinweise für den Orchestrator

- **Modellwahl:** Tasks 1–5 sind mechanisch bzw. gut spezifiziert
  (Standard-Modell; Task 5 ist umfangreich, aber die Zuordnung ist durch
  `de.tsx` eindeutig). Tasks 6–9 (Übersetzungen) und der README-Text in
  Task 12 laufen auf dem **fähigsten verfügbaren Modell** — das ist
  Spec-Vorgabe, kein Optimierungsspielraum. Task 10 ist ein Review-Dispatch,
  ebenfalls fähigstes Modell.
- **Reihenfolge:** 1 → 2 → 3 → 4 → 5 (strikt sequenziell); danach 6–9 in
  beliebiger Reihenfolge (alle hängen nur an 5), 10 nach 9; 11 und 12
  unabhängig nach 5; 13 zuletzt, nach 6, 9, 10, 11 und 12.
- **Definition of Done gesamt:** `npm test` und `npx tsc --noEmit` grün;
  alle fünf Sprachen per Fußzeile schaltbar; deutsche UI pixel-identisch
  zur Fassung vor der Umstellung (Stichprobe); README englisch mit
  aktualisierten Screenshots; Live-Verifikation nach Merge wie gehabt.
